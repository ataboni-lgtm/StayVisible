* In objects, do not use the shorthand if the object key/value variable name are the same. Always do {var1: var1}.
* All Zod objects/schemas should start with z followed by the variable name (e.g. zCreateSystemInput).
* Look through `src/lib/misc-helpers` for helper utilities.