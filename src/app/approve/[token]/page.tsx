import { ApprovalEditor } from '@/components/stay-visible/approval-editor';
import { Logo } from '@/components/stay-visible/logo';
export default async function ApprovalPage({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; return <main className="min-h-screen bg-[#F8FAFC]"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4"><Logo /><span className="text-xs font-medium text-slate-400">Private approval link</span></div></header><div className="@container/page mx-auto max-w-6xl px-4 py-8 @md/page:py-12"><div className="mb-7"><p className="eyebrow">Ready for your review</p><h1 className="mt-2 text-2xl font-semibold text-[#0B1F3A] @md/page:text-3xl">Does this sound like you?</h1><p className="mt-2 text-sm leading-6 text-slate-500">Edit anything you’d like. Your changes help future posts get closer to your voice.</p></div><ApprovalEditor token={token} /></div></main>; }
