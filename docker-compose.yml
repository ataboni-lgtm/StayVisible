name: stay-visible
services:
  stay-visible:
    build: .
    image: stay-visible
    pull_policy: never
    restart: unless-stopped
    user: root
    command: >-
      sh -lc "mkdir -p /app/node_modules /app/.next &&
      chown -R node:node /app/node_modules /app/.next &&
      exec su node -s /bin/sh -c 'npm run dev; tail -f /dev/null'"
    ports:
      - '3005:3005'
    env_file:
      - .env.local
    environment:
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
      - CHOKIDAR_INTERVAL=300
      - NEXT_WEBPACK_USEPOLLING=true
    security_opt:
      - seccomp=unconfined
      - apparmor=unconfined
    cap_add:
      - SYS_ADMIN
    depends_on:
      - postgres
    working_dir: /app
    volumes:
      - .:/app
      - stay-visible-data-volume:/app/data
      - next-node-modules:/app/node_modules

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: stay_visible
      POSTGRES_USER: stay_visible
      POSTGRES_PASSWORD: stay_visible
    ports:
      - '6767:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  stay-visible-data-volume:
  postgres-data:
  next-node-modules:
